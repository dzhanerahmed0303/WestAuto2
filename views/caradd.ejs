<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Auto Toevoegen</title>
    <link rel="stylesheet" href="/reset.css" />
    <link rel="stylesheet" href="/index.css" />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display+SC:ital,wght@0,400;0,700;0,900;1,400;1,700;1,900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Arial", serif;
        background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
        margin: 0;
        padding: 0;
        color: #333;
      }

      main {
        max-width: 1000px;
        margin: 120px auto;
        padding: 50px;
        background-color: #fff;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.25);
        border-radius: 20px;
        animation: fadeIn 0.6s ease-in-out;
      }

      h1 {
        text-align: center;
        color: #111;
        font-size: 2.5rem;
      }

      form {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 25px;
      }

      .form-group {
        display: flex;
        flex-direction: column;
      }

      label {
        font-weight: bold;
        margin-bottom: 6px;
        color: #222;
        font-size: 0.95rem;
      }

      input,
      select,
      textarea {
        padding: 12px;
        border-radius: 10px;
        border: 1px solid #ccc;
        font-size: 1rem;
        transition: 0.3s ease;
        background-color: #f9f9f9;
      }

      input:focus,
      select:focus,
      textarea:focus {
        outline: none;
        border-color: #007bff;
        background-color: #fff;
        box-shadow: 0 0 8px rgba(0, 123, 255, 0.3);
      }

      textarea {
        resize: vertical;
        min-height: 100px;
      }

      .full-width {
        grid-column: 1 / -1;
      }

      /* button {
        grid-column: 1 / -1;
        padding: 16px;
        background: linear-gradient(135deg, #007bff, #0056b3);
        color: #fff;
        font-size: 1.2rem;
        font-weight: bold;
        border: none;
        border-radius: 12px;
        cursor: pointer;
        transition: transform 0.2s ease, background 0.3s ease;
      } */

      /* button:hover {
        transform: scale(1.03);
        background: linear-gradient(135deg, #0056b3, #00408f);
      } */

      .success,
      .error {
        grid-column: 1 / -1;
        text-align: center;
        padding: 14px;
        border-radius: 8px;
        margin-bottom: 15px;
        font-weight: bold;
      }

      .success {
        background-color: #d4edda;
        color: #155724;
      }

      .error {
        background-color: #f8d7da;
        color: #721c24;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <%- include("partials/header") %>
    <main>
      <h1><i class="fa-solid fa-car-side"></i> Nieuwe Auto Toevoegen</h1>

      <% if (success) { %>
      <div class="success"><%= success %></div>
      <% } %> <% if (error) { %>
      <div class="error"><%= error %></div>
      <% } %>

      <form action="/auto-toevoegen" method="POST">
        <div class="form-group">
          <label for="id">ID *</label>
          <input
            type="text"
            id="id"
            name="id"
            placeholder="Bijv. auto123"
            required
          />
        </div>

        <div class="form-group">
          <label for="brand">Merk *</label>
          <input
            type="text"
            id="brand"
            name="brand"
            placeholder="Bijv. Audi"
            required
          />
        </div>

        <div class="form-group">
          <label for="model">Model *</label>
          <input
            type="text"
            id="model"
            name="model"
            placeholder="Bijv. RS6"
            required
          />
        </div>

        <div class="form-group">
          <label for="year">Bouwjaar *</label>
          <input
            type="number"
            id="year"
            name="year"
            placeholder="Bijv. 2022"
            required
          />
        </div>

        <div class="form-group full-width">
          <label for="description">Beschrijving</label>
          <textarea
            id="description"
            name="description"
            placeholder="Korte omschrijving van de auto..."
          ></textarea>
        </div>

        <div class="form-group">
          <label for="owners">Aantal eigenaars</label>
          <input
            type="number"
            id="owners"
            name="owners"
            placeholder="Bijv. 1"
          />
        </div>

        <div class="form-group">
          <label for="fuel">Brandstof</label>
          <input
            type="text"
            id="fuel"
            name="fuel"
            placeholder="Bijv. Benzine"
          />
        </div>

        <div class="form-group">
          <label for="mileage">Kilometerstand</label>
          <input
            type="number"
            id="mileage"
            name="mileage"
            placeholder="Bijv. 5000"
          />
        </div>

        <div class="form-group">
          <label for="hp">PK</label>
          <input type="number" id="hp" name="hp" placeholder="Bijv. 600" />
        </div>

        <div class="form-group">
          <label for="gearbox">Versnellingsbak</label>
          <input
            type="text"
            id="gearbox"
            name="gearbox"
            placeholder="Bijv. Automaat"
          />
        </div>

        <div class="form-group">
          <label for="body_type">Carrosserietype</label>
          <input
            type="text"
            id="body_type"
            name="body_type"
            placeholder="Bijv. Sedan"
          />
        </div>

        <div class="form-group">
          <label for="seats">Aantal zitplaatsen</label>
          <input type="number" id="seats" name="seats" placeholder="Bijv. 5" />
        </div>

        <div class="form-group">
          <label for="doors">Aantal deuren</label>
          <input type="number" id="doors" name="doors" placeholder="Bijv. 4" />
        </div>

        <div class="form-group">
          <label for="engine_size">Motorinhoud (cc)</label>
          <input
            type="number"
            id="engine_size"
            name="engine_size"
            placeholder="Bijv. 3996"
          />
        </div>

        <div class="form-group">
          <label for="weight">Gewicht (kg)</label>
          <input
            type="number"
            id="weight"
            name="weight"
            placeholder="Bijv. 1950"
          />
        </div>

        <div class="form-group">
          <label for="warranty">Garantie</label>
          <select id="warranty" name="warranty">
            <option value="">Selecteer...</option>
            <option value="true">Ja</option>
            <option value="false">Nee</option>
          </select>
        </div>

        <div class="form-group">
          <label for="emission_class">Emissieklasse</label>
          <input
            type="text"
            id="emission_class"
            name="emission_class"
            placeholder="Bijv. Euro 6"
          />
        </div>

        <div class="form-group">
          <label for="co2emissions">CO2-uitstoot (g/km)</label>
          <input
            type="number"
            id="co2emissions"
            name="co2emissions"
            placeholder="Bijv. 220"
          />
        </div>

        <div class="form-group">
          <label for="colour">Kleur</label>
          <input
            type="text"
            id="colour"
            name="colour"
            placeholder="Bijv. Zwart"
          />
        </div>

        <div class="form-group full-width">
          <label for="image">Afbeeldings-URL</label>
          <input
            type="url"
            id="image"
            name="image"
            placeholder="Bijv. /NewFotos/Audi-RS6.png"
          />
        </div>

        <button type="submit">
          <i class="fa-solid fa-plus"></i> Auto Toevoegen
        </button>
      </form>
    </main>
    <%- include("partials/footer") %>
  </body>
</html>
